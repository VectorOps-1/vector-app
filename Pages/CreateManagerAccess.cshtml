@page
@model CreateManagerAccessModel
@using vector_app_local.Services
@inject IWebHostEnvironment WebHostEnvironment
@{
    ViewData["Title"] = "Create Manager Access";
    var companyLogoPath = CompanyBranding.GetLogoPath(WebHostEnvironment);
    var companyName = CompanyBranding.GetCompanyName(WebHostEnvironment);
}

<div class="access-screen">
    <div class="access-inner">
        <img class="page-logo" src="@companyLogoPath" alt="Company Logo" />
        <div class="client-line">Client: @companyName</div>
        <div class="home-label">Senior Management</div>
        <h2 class="page-title">Create Manager Access</h2>
        <p class="page-subtitle">Create manager login details and assign access permissions</p>

        <form class="access-form">
            <section class="form-panel">
                <div class="section-label">Manager Details</div>
                <div class="form-grid">
                    <div class="field-group">
                        <label class="field-label">Full name</label>
                        <input class="text-input" type="text" placeholder="Manager full name" />
                    </div>

                    <div class="field-group">
                        <label class="field-label">Email</label>
                        <input class="text-input" type="email" placeholder="manager@example.com" />
                    </div>

                    <div class="field-group">
                        <label class="field-label">Mobile number</label>
                        <input class="text-input" type="tel" placeholder="+27 00 000 0000" />
                    </div>

                    <div class="field-group">
                        <label class="field-label">Role / title</label>
                        <input class="text-input" type="text" placeholder="Operations Manager" />
                    </div>

                    <div class="field-group">
                        <label class="field-label">Department / base / location</label>
                        <select class="text-input">
                            <option value="">Select base / location</option>
                            @foreach (var location in Model.LocationOptions)
                            {
                                <option value="@location.Value">@location.Text</option>
                            }
                        </select>
                    </div>

                    <div class="field-group">
                        <label class="field-label">Access level</label>
                        <select class="text-input">
                            <option>Operational Management</option>
                            <option>Senior Management</option>
                        </select>
                    </div>

                    <div class="field-group">
                        <label class="field-label">Temporary password</label>
                        <input class="text-input" type="password" placeholder="Set temporary password" />
                    </div>

                    <label class="toggle-row">
                        <input type="checkbox" checked />
                        <span>Account active</span>
                    </label>
                </div>
            </section>

            <section class="form-panel permissions-panel">
                <div class="section-label">Permissions</div>
                <div class="permission-grid">
                    <label class="permission-item"><input type="checkbox" checked /> Company Profile</label>
                    <label class="permission-item"><input type="checkbox" checked /> Upload Checklists</label>
                    <label class="permission-item"><input type="checkbox" checked /> Upload Registers</label>
                    <label class="permission-item"><input type="checkbox" checked /> Staff Records</label>
                    <label class="permission-item"><input type="checkbox" checked /> Vehicle Records</label>
                    <label class="permission-item"><input type="checkbox" checked /> Equipment Records</label>
                    <label class="permission-item"><input type="checkbox" checked /> Stock Records</label>
                    <label class="permission-item"><input type="checkbox" /> Send Tasks to Staff</label>
                    <label class="permission-item"><input type="checkbox" /> View Reports</label>
                    <label class="permission-item"><input type="checkbox" /> Edit / Delete Records</label>
                </div>
            </section>

            <div class="action-row">
                <button class="menu-btn equipment-btn" type="button" data-action-confirmation data-confirmation-message="Manager access created.">Create Access</button>
                <button class="menu-btn vehicle-btn" type="button" data-action-confirmation data-confirmation-message="Manager invite sent.">Send Invite</button>
            </div>
        </form>
    </div>
</div>

<style>
.access-screen {
    display: flex;
    width: 100%;
    min-height: calc(100vh - 70px);
    justify-content: center;
    align-items: flex-start;
}

.access-inner {
    width: 100%;
    max-width: 1080px;
    margin: 0 auto;
    text-align: center;
    padding: 40px 20px 30px 20px;
    box-sizing: border-box;
}

.page-logo {
    width: 300px;
    max-width: 72%;
    max-height: 170px;
    object-fit: contain;
    margin: 0 auto 12px auto;
    display: block;
}

.client-line {
    margin-bottom: 14px;
    font-size: 12px;
    font-weight: 700;
    color: #6b7280;
}

.home-label {
    margin-bottom: 10px;
    font-size: 13px;
    font-weight: 600;
    letter-spacing: 1.2px;
    text-transform: uppercase;
    color: #5f6b7a;
}

.page-title {
    margin: 0 0 8px 0;
    font-size: 28px;
    font-weight: 700;
    color: #2f2f2f;
}

.page-subtitle {
    margin: 0 0 28px 0;
    font-size: 16px;
    font-weight: 500;
    color: #6b7280;
}

.access-form {
    display: flex;
    flex-direction: column;
    gap: 22px;
}

.form-panel {
    padding: 22px;
    border: 1px solid rgba(66, 84, 102, 0.18);
    border-radius: 8px;
    background: rgba(255, 255, 255, 0.58);
    text-align: left;
}

.section-label {
    margin-bottom: 16px;
    font-size: 13px;
    font-weight: 800;
    letter-spacing: 1.1px;
    text-transform: uppercase;
    color: #5f6b7a;
}

.form-grid,
.permission-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 18px 24px;
}

.field-group {
    text-align: left;
}

.field-label {
    display: block;
    margin-bottom: 8px;
    font-size: 13px;
    font-weight: 700;
    color: #425466;
}

.text-input {
    width: 100%;
    padding: 14px 16px;
    border: 1px solid rgba(66, 84, 102, 0.24);
    border-radius: 8px;
    background: #ffffff;
    font-size: 16px;
    color: #111827;
    box-sizing: border-box;
}

.toggle-row,
.permission-item {
    display: flex;
    align-items: center;
    gap: 12px;
    min-height: 48px;
    font-size: 15px;
    font-weight: 700;
    color: #425466;
}

.toggle-row input,
.permission-item input {
    width: 20px;
    height: 20px;
}

.action-row {
    display: flex;
    justify-content: center;
    gap: 24px;
    flex-wrap: wrap;
}

.action-row .menu-btn {
    min-width: 260px;
    border: none;
}

@@media (max-width: 780px) {
    .form-grid,
    .permission-grid {
        grid-template-columns: 1fr;
    }
}
</style>
