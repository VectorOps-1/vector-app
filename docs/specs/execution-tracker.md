# AcuityOps Spec Execution Tracker

This tracker is the execution gate for `docs/specs/acuityops-master-build-spec.md`.

The master spec remains the product and architecture authority. This tracker controls sequence, authorization, status, verification, and anti-deviation behavior. Product source code, database files, runtime files, generated files, and app behavior must not be changed unless the user authorizes one exact tracker row.

## Mandatory Anti-Deviation Rules

1. Do not work from memory. Read the relevant tracker row and master spec section before acting.
2. Do not invent step numbers. If the user asks for `Step 10` and the current tracker does not contain that step in the active phase, stop and report the mismatch.
3. Do not combine tracker rows. One user instruction may complete one tracker row unless the user explicitly authorizes a named batch of tracker rows.
4. Do not edit product source code during spec, tracker, audit, or planning rows.
5. Do not touch database files, runtime files, generated files, app startup, or app process state unless the active tracker row explicitly permits it.
6. Do not treat prior untracked work as complete. Prior work must be reconciled against the tracker before any row is marked `Done`.
7. Do not mark a row `Done` without the required verification evidence.
8. Do not continue to the next row automatically. Stop, report status, and provide the exact next instruction for the user to give.
9. If new work is discovered, add it as a proposed tracker row or report it as a required tracker change. Do not implement it silently.
10. If a row conflicts with the master spec, stop and request a spec/tracker correction before implementation.

## Allowed Status Values

- `Not started`
- `In progress`
- `Blocked`
- `Done`

## Required Pre-Flight Before Any Product Work

Before any product source, database, runtime, or generated-file work, Codex must state:

1. Active tracker row ID and exact title.
2. Matching master spec section.
3. Files allowed to change.
4. Files forbidden to change.
5. Whether database access is read-only, write-enabled, or forbidden.
6. Whether app startup/browser verification is allowed.
7. Required verification commands or checks.
8. Stop condition.

If any answer is unclear, the row is `Blocked`.

## Required Post-Step Report

After each authorized row, Codex must report:

1. Tracker row ID and title.
2. Final row status.
3. Files changed.
4. Verification completed.
5. Verification not run and reason.
6. Remaining risk.
7. App-readiness percentage where `100%` means a high-level production SaaS app running with real clients.
8. Exact next recommended instruction.

## Current Active Row

No implementation row is active.

The next implementation instruction must name one row from the tracker below.

## Phase 1: Workspace And Data Hygiene

| ID | Status | Master Spec Step | Authorized Scope | Verification Required | Notes |
| --- | --- | --- | --- | --- | --- |
| P1-01 | Done | Run full git/worktree audit and separate source files from runtime, generated, and database files. | Read-only audit only. | Git/worktree report. | Reconciled by git status, ignored-file scan, tracked-runtime scan, and source/runtime split review before Phase 1 checkpoint. |
| P1-02 | Done | Update `.gitignore` so SQLite files, backups, logs, `bin/`, `obj/`, artifacts, uploads, and runtime clutter do not appear in git status. | Hygiene docs/config only. | Git status before/after. | Hygiene committed in `c70ab96`; ignored runtime/database clutter verified after cleanup. |
| P1-03 | Done | Remove tracked generated files from git index without deleting local runtime files. | Git index cleanup only. | Git status before/after. | Tracked generated/runtime/database scan returned no tracked `bin`, `obj`, `artifacts`, logs, uploads, or SQLite files. |
| P1-04 | Done | Create a clean source checkpoint commit for hygiene only. | Commit hygiene slice only. | Commit hash and clean/expected status. | Hygiene checkpoint exists at `c70ab96`; spec guardrails committed separately at `c350d35`. |
| P1-05 | Done | Stop all normal startup/sample seed mutation. | Source changes limited to startup/seed paths. | Build and source scan. | Normal startup mutation removed; development repair remains explicit via `--dev-db-repair`; source checkpoint committed at `dbc4b30`; build passed with 0 warnings and 0 errors. |
| P1-06 | Done | Remove schema/data repair calls from normal page requests. | Source changes limited to request-time repair paths. | Build and route/source scan. | Legacy route/request repair cleanup committed at `dbc4b30`; route/source scan found no normal page-request repair flow; build passed with 0 warnings and 0 errors. |
| P1-07 | Done | Preserve current login accounts, but remove stale seeded company, register, checklist, schematic-assignment, and readiness artifacts from the active dev database. | Database cleanup only after backup. | Backup evidence, cleanup report, login verification. | Active dev database was backed up to `vector-dev.backup-before-p1-07-seed-artifact-cleanup-20260621-060810.db`; login records preserved; stale seeded company/register/checklist/schematic-assignment/readiness artifacts removed. |
| P1-08 | Done | Verify the app starts cleanly without recreating old seed data. | Verification only unless failures require a new tracker row. | App start, browser verification, seed/fallback scan. | Controlled `http://localhost:5000` verification completed with `P1-08 failures: none`; live empty states and no seed recreation were verified. |

## Phase 1 Closure Evidence

- Hygiene/spec commits: `c70ab96 chore: clean repository hygiene`, `f0ec80c docs: add AcuityOps master build spec`, `c350d35 Add spec execution tracker guardrails`.
- Register-driven checklist and legacy-route cleanup commits: `0729f80 fix: redirect legacy app routes`, `9ccbda2 fix: enforce register-driven checklist starts`, `dbc4b30 chore: checkpoint phase 1 cleanup`.
- Build verification: `dotnet build vector-app-local.csproj` completed with 0 warnings and 0 errors before the Phase 1 cleanup checkpoint commit.
- Database cleanup evidence: active dev database backup `vector-dev.backup-before-p1-07-seed-artifact-cleanup-20260621-060810.db` was created before stale seed artifact cleanup.
- Runtime verification evidence: P1-08 controlled verification on `http://localhost:5000` reported `P1-08 failures: none`.
- Current Phase 1 close condition: source tree was clean after `dbc4b30` before this docs-only tracker reconciliation.

## Phase Gates Requiring Child-Step Expansion Before Implementation

The rows below are locked phase gates. Before implementation starts inside any of these phases, Codex must expand the phase into child tracker rows from the master spec and wait for user authorization.

| ID | Status | Master Spec Section | Gate Requirement |
| --- | --- | --- | --- |
| P2-GATE | Done | Phase 2: Company And Tenant Source Of Truth | Expanded into child rows `P2-09` through `P2-15`; implementation remains locked until one child row is explicitly authorized. |
| P2A-GATE | Not started | Phase 2A: New Client Setup Wizard | Expand steps 15A-15R into tracker rows before implementation. |
| P3-GATE | Not started | Phase 3: Access And Permissions | Expand steps 16-23 into tracker rows before implementation. |
| P4-GATE | Not started | Phase 4: Navigation And Route Cleanup | Expand steps 24-30 into tracker rows before implementation. |
| P5-GATE | Not started | Phase 5: Checklist Source Of Truth | Expand steps 31-38 into tracker rows before implementation. |
| P6-GATE | Not started | Phase 6: Checklist Builder And Publishing | Expand steps 39-47 into tracker rows before implementation. |
| P7-GATE | Not started | Phase 7: Live Daily Checks | Expand steps 48-55 into tracker rows before implementation. |
| P8-GATE | Not started | Phase 8: PDF Evidence | Expand steps 56-61 into tracker rows before implementation. |
| P9-GATE | Not started | Phase 9: Registers And Assets | Expand steps 62-70 into tracker rows before implementation. |
| P10-GATE | Not started | Phase 10: Stock And Medication Flow | Expand steps 71 and onward from the master spec before implementation. |
| P11-GATE | Not started | Phase 11: Schematics | Expand schematic assignment and live schematic behavior into tracker rows before implementation. |
| P11A-GATE | Not started | Phase 11A: Global Vehicle Schematic Library Completion | Expand each schematic asset batch into tracker rows before generation or app integration. |
| P12-GATE | Not started | Phase 12: Readiness And Reports | Expand readiness/reporting rows before implementation. |
| P12B-GATE | Not started | Phase 12B: South African EMS Audit Compliance Mode | Expand legal/source-pack/review rows before implementation. |
| P12C-GATE | Not started | Phase 12C: South African Regulatory Source Pack And Multi-Country Compliance Architecture | Expand source compilation and jurisdiction model rows before implementation. |
| P12D-GATE | Not started | Phase 12D: SMS And Email Notification Engine | Expand provider, template, consent, audit, and retry rows before implementation. |
| P13-GATE | Not started | Phase 13: AI Import | Expand AI import, mapping, validation, and review rows before implementation. |
| P13A-GATE | Not started | Phase 13A: Clinical Guideline And SOP Knowledge UI | Expand document ingestion, source citation, UI, permission, and tier rows before implementation. |
| P14-GATE | Not started | Phase 14: AI Predictive Analysis | Expand prediction inputs, outputs, safety, reports, and validation rows before implementation. |
| P15-GATE | Not started | Phase 15: SaaS Packaging | Expand tenant packaging rows before implementation. |
| P15A-GATE | Not started | Phase 15A: Commercial Downgrade, Cancellation, And Tier Data Rules | Expand downgrade/cancellation behavior rows before implementation. |
| P15B-GATE | Not started | Phase 15B: Exact Tier Feature Matrix And Data-State Rules | Expand tier lock/read-only/export/delete behavior rows before implementation. |
| P15C-GATE | Not started | Phase 15C: Production Billing, Payment Provider, Tax, And Invoice Architecture | Expand billing provider and subscription state rows before implementation. |
| P16-GATE | Not started | Phase 16: Azure And Production | Expand Azure production architecture rows before implementation. |
| P16A-GATE | Not started | Phase 16A: Release Isolation And Client-Specific Publishing | Expand environment, tenant release, and deployment rows before implementation. |
| P16B-GATE | Not started | Phase 16B: Mandatory Security And Client Data Protection Sweeps | Expand security, encryption, backup, and access review rows before implementation. |
| P16C-GATE | Not started | Phase 16C: Mandatory Legal, Compliance, Evidence, And IP Review | Expand legal review and evidence retention rows before implementation. |
| P16D-GATE | Not started | Phase 16D: Client Data Portability, Export, And Offboarding | Expand export/delete/offboarding rows before implementation. |
| P16E-GATE | Not started | Phase 16E: Tenant Isolation And Multi-Client Provisioning Decision Record | Expand tenant ID, database, storage, domain, and backup rows before implementation. |
| P16F-GATE | Not started | Phase 16F: Production Observability And Incident Response Decision Record | Expand logs, metrics, alerts, uptime, support, and incident review rows before implementation. |
| P16G-GATE | Not started | Phase 16G: Production Support And Client-Success Operating Model | Expand onboarding, helpdesk, SLA, training, documentation, and feedback rows before implementation. |
| P16H-GATE | Not started | Phase 16H: Production Sales, Demo, And Trial-Conversion Operating Model | Expand sales/demo/trial conversion controls before implementation. |
| P16I-GATE | Not started | Phase 16I: Production Website And Marketing-Content Truth Control | Expand website claims, pricing, demo request, SEO, analytics, and legal review rows before implementation. |
| P17-GATE | Not started | Phase 17: Website And Launch | Expand website execution and launch rows after Phase 16I truth-control is done. |
| P18-GATE | Not started | Phase 18: Final Release Gate | Expand final audit, release, tagging, and production verification rows before implementation. |

## Phase 2: Company And Tenant Source Of Truth

These rows come from `docs/specs/acuityops-master-build-spec.md`, Phase 2 steps 9-15 and the mandatory Phase 2 expansion block. Phase 2 must make the company/tenant settings record the only source of truth for company name, workspace identity, logo, and logged-in branding. No row below may reintroduce seed branding, file-based branding, company-name string matching as a security boundary, or tenant leakage.

| ID | Status | Master Spec Step | Authorized Scope | Verification Required | Notes |
| --- | --- | --- | --- | --- | --- |
| P2-09 | Done | Make Master Setup the only source of company name, logo, workspace settings, and branding. | Completed in commit `3373d72` with product source and migration changes only. Active dev schema was backed up and migration-applied separately for verification; no company data cleanup was included. | Build passed before commit. Schema application verified `CompanyLogin` and `Access` no longer throw missing `BrandingStatus` errors. Authenticated senior verification confirmed `Home`, `CompanyProfile`, `CompanyName`, and `LogoUpload` return 200; `X Med` did not render on verified pages. | Company settings now own company name, trading name, workspace slug, logo reference, logo removed state, country, timezone, and branding status. Logo remove UI is conditional on a stored logo being present, so it was not submitted or forced during verification. |
| P2-10 | Done | Remove all X Med or seed branding restoration paths. | Completed in commit `1f59ce6` with product source-controlled launcher changes only. No database writes, seed repair commands, runtime files, generated files, or app startup were included. | Source scan found no app-owned `X Med`, `x-med`, `Random Med`, or `Client Business Name` references in `Data`, `Models`, `Pages`, `Services`, `Program.cs`, `wwwroot` excluding vendor libraries, or launcher scripts. `dotnet build vector-app-local.csproj` passed with 0 warnings and 0 errors. | Removed stale `x-med` workspace links from local launcher output so normal development guidance no longer directs testing to seed-branded workspace URLs. |
| P2-11 | Done | Keep AcuityOps branding on pre-company-login screens. | Completed in commit `e002f01` with product source/UI changes only. No database data, runtime files, generated files, or tenant records were touched. | Pre-auth HTTP verification confirmed `/`, `/Access`, `/CompanyLogin`, and `/CompanyLogin/x-med-workspace` render AcuityOps branding with no detected tenant logo/name leakage. Pre-auth `/RoleLogin?access=senior-management` redirects to `/CompanyLogin`. | Access and role login no longer select the first development company when no authenticated company session exists, preventing tenant branding before company authentication. |
| P2-12 | Done | Make uploaded logos persist and render across all logged-in pages. | Completed in commit `e5d0628` with product source/storage-path handling changes only. No database, backup, runtime, generated, or log files were committed. | `dotnet build vector-app-local.csproj` passed with 0 warnings and 0 errors. Controlled logo write verification backed up the active dev database, logged in through the existing senior test flow, uploaded a temporary PNG, verified Home and App Guide rendered `/uploads/company/1/company-logo.png?v=...`, replaced the logo, verified the cache-busted `?v=` value changed, then restored the database logo state and removed temporary upload files. | Uploaded logos now use the canonical company-scoped storage path, logged-in Home/App Guide use the shared company branding context, stale `Vector.CompanyName` session writes were removed, and logged-in pages render company name/logo from the settings record path only. |
| P2-13 | Done | Add remove-logo functionality. | Completed in commit `41a08d5` with product source/UI and stored logo reference handling changes only. No database, backup, runtime, generated, or log files were committed. | `dotnet build` passed with 0 warnings and 0 errors. Controlled remove-logo write verification backed up the active dev database, logged in through the existing senior test flow, uploaded a temporary transparent PNG, verified cache-busted logo rendering, verified the Remove Logo form is wired to the in-app confirmation modal, submitted the remove action, verified `LogoStoragePath = null` and `LogoRemoved = 1`, verified logged-in Home fell back to default AcuityOps branding, then restored the previous database and logo file state. | Removing a logo clears the stored reference and does not restore X Med or previous seed/default tenant branding. |
| P2-14 | Done | Verify company name and logo changes survive restart and reload. | Controlled verification completed with no product source, database schema, runtime, generated, staging, or commit changes before this tracker reconciliation. Active dev database was backed up to `vector-dev.backup-before-p2-14-company-identity-20260621224758.db`, then restored after verification. | Verified through the real company access and senior login flow: temporary company identity was saved through Company Identity, temporary PNG logo was uploaded through Logo Upload, logged-in Home rendered the temporary name and cache-busted logo after save and reload, the app was restarted, senior login was repeated, and logged-in Home still rendered the temporary name and logo. Final restore confirmed blank company name, no stored logo reference, `BrandingStatus = Incomplete`, absent logo folder, stopped app, and clean Git status. | Empty company identity remains restored for the current dev database. Company name/logo persistence across reload and restart is verified for the coded Master Setup source-of-truth path. |
| P2-15 | Done | Confirm company data is scoped by tenant/company ID everywhere. | Gate expanded into child rows `P2-15A` through `P2-15M`; no product source, database, runtime, generated files, or app startup changes were made during gate expansion. | Child rows below must be completed before Phase 2 can close. | Company names and workspace slugs must not be used as tenant isolation boundaries. Production multi-client architecture remains governed by `P16E-GATE`; these rows verify and fix the current app's tenant/company scoping. |
| P2-15A | Done | Confirm every tenant-owned data model has an approved tenant/company key. | Read-only model and migration audit completed. Most top-level tenant-owned models have direct `CompanyId`: users, permissions, audit logs, tasks, issues, areas, asset movements/files, vehicles, schematic assignments, equipment, stock, medication, checklist templates, readiness records, dropdowns, and catalogue items. No source, database, runtime, generated, staging, commit, or app-start changes were made during the audit. | Gaps reported: checklist child models are parent-scoped through `ChecklistTemplateId` / `ChecklistSectionId` / `ChecklistItemId` and must be query-verified in `P2-15B`; root/baseline EF migration and model snapshot are missing; `TaskEvent` and `UploadedFile` exist in the model/context without normal migrations; several checklist schema tables still appear in `Data/DevelopmentDatabase.cs` repair code rather than canonical migrations. | `P2-15H` is now unblocked for schema/key remediation planning. `P2-15B` remains required before parent-scoped child records can be accepted as tenant-safe in use. Global product configuration may be company-independent only when it contains no client data. Product-owned schematic library assets may be global; schematic assignments are tenant-owned. |
| P2-15B | Done | Confirm every page, handler, and service query scopes tenant-owned data by current company/tenant. | Read-only source audit completed across `Pages`, `Services`, shared helpers, and tenant-owned query paths. No product source, database, runtime, generated, staging, commit, delete, or app-start changes were made during the audit. Most register, staff, file, schematic, checklist report, readiness dashboard, readiness metric, operational report, movement, setup upload, expiry pressure, and edit-item paths inspected were company-scoped or safely parent-scoped through a company-scoped root. | Blocking tenant-scope gaps reported for `Services/CurrentUserService.cs:70`, `Pages/TaskAction.cshtml.cs:38`, `Pages/TaskInbox.cshtml.cs:49`, `Pages/TaskInbox.cshtml.cs:99`, `Pages/TaskFeedback.cshtml.cs:122`, `Pages/TaskFeedback.cshtml.cs:241`, `Pages/TaskFeedback.cshtml.cs:272`, `Pages/TaskNotificationCount.cshtml.cs:30`, `Pages/IssueInbox.cshtml.cs:41`, `Pages/IssueInbox.cshtml.cs:71`, `Pages/IssueNotificationCount.cshtml.cs:29`, `Pages/IssueReportAction.cshtml.cs:77`, `Services/SessionAccessPageFilter.cs:504`, `Services/ChecklistVarianceService.cs:24`, `Services/ReadinessAlertService.cs:25`, and weak reload at `Services/ChecklistPublishingService.cs:165`. | `P2-15I` is now unblocked for query/action scoping fixes. The fixes must add current-company validation to task and issue inboxes, notification counts, task-access override, post handlers, current-user resolution, report alert creation entry points, and checklist retirement reloads. Navigation hiding remains insufficient; server-side company filtering is mandatory before rendering or mutating tenant-owned records. |
| P2-15C | Done | Confirm workspace login, role login, session, and tenant resolution are tenant-safe. | Read-only source audit completed for workspace slug resolution, company login, role login, session keys, current-user context, redirects, invalid workspace behavior, and pre-auth branding. No product source, database, runtime, generated, staging, commit, delete, or app-start changes were made during the audit. | Blocking tenant/session gaps reported: `Pages/CompanyLogin.cshtml.cs:53` and `Pages/CompanyLogin.cshtml:31` expose the company access code by pre-filling it into the password field; `Services/CurrentUserService.cs:70` loads the current user by user ID only and `Services/CurrentUserService.cs:44` lets that user's company override session company context; `Services/SessionAccessPageFilter.cs:150` checks user ID/access view without validating the user belongs to the selected company session; invalid workspace paths at `Pages/CompanyLogin.cshtml.cs:45` and `Pages/CompanyLogin.cshtml.cs:64` do not clear stale company/user session state; `/Access` at `Pages/Access.cshtml.cs:15` can render without a selected company session. | `P2-15I` must fix session/user/company binding, stale-session cleanup, and company-login access-code exposure. `P2-15E` must continue deeper cache/session/state review. Passed checks: workspace lookup uses stored `WorkspaceSlug` plus active status, RoleLogin scopes user lookup by selected company, and Index/Access/Workspace/CompanyLogin keep AcuityOps pre-auth branding. Workspace slugs and company names remain non-security boundaries. |
| P2-15D | Done | Confirm upload and file paths are tenant-scoped. | Read-only source audit completed for logos, staff files, register imports, checklist imports, PDF output, report artifact usage, schematic assets and assignments, `wwwroot/uploads`, and local storage path usage. No product source, database, runtime, generated, staging, commit, delete, or app-start changes were made during the audit. | Blocking storage gaps reported: tenant-owned uploads use `IFileStorageService.SaveAsync(IFormFile file, string category)` with no company/tenant argument, and `LocalFileStorageService` stores files under shared `App_Data/uploads/{category}/{yyyy}/{MM}/{guid}` rather than a tenant-scoped physical path. Staff files, setup/register/checklist import files, and task feedback evidence write company-scoped metadata to `AssetFile`, but the stored path itself is not tenant-isolated. Upload validation gaps reported: setup imports, staff uploads, add-staff uploads, and task evidence rely on extension allowlists without consistent max-size caps, MIME/signature validation, or malware/security scan integration. Logo upload is company-path scoped and has PNG signature and 4 MB checks, but it is publicly served from `/uploads/company/{companyId}/company-logo.png`; classify it as public branding or move it behind authenticated delivery. Checklist PDF downloads are generated in memory after company/access checks and do not create persistent shared PDF files. Product-owned schematic image assets are global and acceptable; schematic assignments are company-scoped. Dormant `UploadedFile` is not used by current upload flows but must not be used later without direct or parent-enforced tenant scoping. | `P2-15J` must add tenant-scoped file-storage paths/API, per-upload size limits, content validation, security scan integration point, and an explicit decision for public logo handling. Product-owned static schematics may remain global; client-owned files must not use shared category-only storage paths. |
| P2-15E | Done | Confirm cache, session, temporary state, and background/task state cannot cross tenants. | Read-only source audit completed for session keys, TempData, browser local/session storage, static collections, notification count endpoints, task-access state, feature tier state, and background/cache patterns. No product source, database, runtime, generated, staging, commit, delete, or app-start changes were made during the audit. | Blocking tenant-state gaps reported: `Services/CurrentUserService.cs:59` loads current user by user ID only and `Services/CurrentUserService.cs:42` returns the user's company before validating session company; `Services/SessionAccessPageFilter.cs:148` authorizes by user ID/access view without tenant validation; `Services/SessionAccessPageFilter.cs:501` task-access override checks task ID, assigned user, and status without `CompanyId`; `Pages/TaskNotificationCount.cshtml.cs:30` and `Pages/IssueNotificationCount.cshtml.cs:29` query assigned task/issue counts without tenant filters; `Pages/DailyVehicleChecklist.cshtml.cs:141`, `wwwroot/js/site.js:537`, `Pages/DailyVehicleChecklist.cshtml:497`, and `Pages/EditVehicleChecklist.cshtml:942` use browser storage keys that are not tenant-prefixed. | No tenant-specific static mutable collection, app-owned background queue, hosted background service, or shared cache object holding tenant data was found. `P2-15I` must fix session/user/company binding, task-access scoping, task/issue notification scoping, feature tier consistency, and tenant-prefixed browser storage. Production must replace local in-memory session backing at `Program.cs:20` with a deployment-suitable distributed/session strategy before SaaS release. |
| P2-15F | Done | Confirm product-owned global library data is separated from tenant-owned assignments. | Read-only source audit completed for vehicle schematic product assets, schematic assignment service/model/page flows, readiness product defaults, checklist templates and publish scopes, setup upload/source records, catalogue items, custom dropdown defaults, and live checklist/register schematic resolution. No product source, database, runtime, generated, staging, commit, delete, or app-start changes were made during the audit. | Passed separation checks: schematic image files and `VehicleSchematicLibrary.All` are product-owned reference assets with no company/register/checklist/branding data; `VehicleSchematicAssignment` stores tenant-owned assignments with `CompanyId`; live daily checks and vehicle register resolve schematics through company-scoped assignment service; readiness product defaults are copied into a company draft only when senior management intentionally applies them; auto-populated readiness suggestions read only current-company registers and start inactive; checklist templates and catalogue items are company-owned; setup imports are recorded as company-scoped `AssetFile` metadata; custom dropdown additions are company-scoped. | Blocking/remediation findings: `VehicleSchematicLibrary.LegacyAliases` can silently map stale labels such as `ambulance`, `ops-ambulance`, or `response-sedan` into product schematic keys; checklist builder still has hidden product schematic defaulting through `ResolveDefaultSchematicKey` and client-side `defaultUnitSchematicKey`; `ChecklistPublishScope` remains parent-scoped without a direct `CompanyId`; dormant legacy `Vehicle.SchematicType` remains; dormant `UploadedFile` remains parent-scoped and must not be used without direct or parent-enforced tenant scope. `P2-15J` must remove global-library assignment/default gaps, and `P2-15H` must cover remaining schema/key risks. |
| P2-15G | Done | Confirm seed, fallback, demo, repair, and startup routines cannot create or restore tenant records. | Read-only source audit completed for startup, dev repair/reset command gates, route fallbacks, compatibility shims, default checklist/rendering fallbacks, demo/sample strings, readiness defaults, schematic assignment paths, and checklist register/daily-check source-of-truth paths. No product source, database files, runtime files, generated files, staging, commit, delete, or app-start changes were made during the audit. | Passed checks: normal startup does not seed or repair data; `Program.cs` gates `DevelopmentDatabase.RepairSqliteDevelopmentSchemaAsync` behind explicit `--dev-db-repair`; no normal page-request schema repair caller was found; no migration `InsertData` / `HasData` seed path was found; removed legacy route files for `/DailyChecklist`, `/DailyEquipmentChecklist`, `/EditEquipmentChecklist`, and `/ManagerAreas` remain absent; live daily checks use company-scoped published checklist templates and explicitly exclude `Seed`, `Default`, and `Sample` source types; `ChecklistTemplateView` is read-only and company-scoped. Findings requiring later cleanup: `Data/DevelopmentDatabase.cs` remains a broad dev repair utility and must stay impossible to run in production; `Services/VehicleSchematicLibrary.cs` still has `LegacyAliases` that silently map stale schematic labels to product schematic keys; `Pages/EditVehicleChecklist.cshtml.cs` and `Pages/EditVehicleChecklist.cshtml` still have hidden non-scratch default schematic selection through `DefaultUnitSchematicKey` / `ResolveDefaultSchematicKey` / `defaultUnitSchematicKey`; `Pages/EditVehicleChecklist.cshtml.cs` still has legacy checklist name and publish-scope compatibility mapping; Readiness Engine product defaults and auto-populated rules are explicit senior POST actions, not startup mutation, but must remain setup-controlled and audited. | Normal startup and normal page requests did not show code paths that can recreate tenant records, but compatibility/defaulting cleanup remains required before tenant isolation can be considered production-safe. `P2-15I` and `P2-15J` must cover the remaining session/query/default-schematic/global-library gaps; `P2-15H` must govern repair/schema utility risk. |
| P2-15H | Done | Fix tenant-key/schema gaps found by `P2-15A`. | Implemented in commit `4d669f0` (`Add tenant keys for checklist scope events uploads`). Added direct `CompanyId` tenant keys and company relationships for `ChecklistPublishScope`, `TaskEvent`, and dormant `UploadedFile`; added company navigation collections; updated EF mappings and tenant-aware indexes; updated checklist publish and task-event writers to persist `CompanyId`; tightened checklist publish-scope queries to filter by direct `CompanyId`; aligned explicit dev-only schema repair shape so it cannot recreate tenant-less versions of those tables; added migration `20260622110000_AddTenantKeysForChecklistScopesTaskEventsAndUploads`. No active database data, runtime files, generated files, or app startup changes were made during implementation. | `dotnet build` passed with 0 warnings and 0 errors after implementation. Migration review confirmed the new migration backfills `ChecklistPublishScopes.CompanyId` from parent checklist templates and creates canonical `TaskEvents` / `UploadedFiles` schema with direct tenant keys. | Root/baseline EF model snapshot remains absent and requires a separate migration-history/baseline decision before production migration discipline can be considered complete. `P2-15I` remains required for query/session/action scoping gaps; `P2-15J` remains required for tenant file-storage and global-library defaulting gaps. |
| P2-15I | Done | Fix tenant query/action scoping gaps found by `P2-15B`, `P2-15C`, and `P2-15E`. | Implemented in commit `509d35a` (`Fix tenant query and session scoping`). Product source changes were limited to the reported current-user, company-login, access-filter, task, issue, checklist-publishing, checklist-variance, readiness-alert, and browser-state scoping gaps. No database data, runtime files, generated files, logs, or app startup changes were made during implementation or commit. | `dotnet build` passed with 0 warnings and 0 errors after implementation. Targeted scans confirmed task and issue actions/counts now scope by current company, task-access override now requires current company, current user resolution requires both session user and company, company access code is not prefilled, checklist retirement reloads by current company, alert creation entry points validate the performer/report company, and daily-check/checklist-builder browser storage keys are tenant-prefixed with no unscoped draft fallback. | P2-15I closes the reported query/action/session/state scoping gaps. Remaining tenant isolation work is still governed by `P2-15J` for tenant file-storage and global-library assignment/default gaps, `P2-15K` for automated tenant-isolation tests, `P2-15L` for controlled two-tenant verification, and `P2-15M` for Phase 2 closure. |
| P2-15J | Done | Fix tenant file-storage and global-library assignment gaps found by `P2-15D` and `P2-15F`. | Implemented in commit `73ec695` (`Fix tenant file storage and schematic defaults`). Tenant-owned uploads now require `companyId` and write future files under tenant-scoped `local/company-{id}/...` storage paths; setup imports, staff documents, personal documents, staff-file uploads, and task-feedback evidence pass explicit upload policies. Added central size limits, extension validation, content signature validation, typed validation errors, and an `IFileSecurityScanner` integration point with `NoOpFileSecurityScanner` as the current local implementation. Documented company logos as public branding assets while protected client uploads use tenant-scoped storage. Removed `VehicleSchematicLibrary.LegacyAliases` and removed checklist-builder hidden default schematic selection through `DefaultUnitSchematicKey`, `ResolveDefaultSchematicKey`, and client-side `defaultUnitSchematicKey`. No active database data, runtime upload files, generated files, logs, or app startup changes were made during implementation or commit. | `dotnet build` passed with 0 warnings and 0 errors. Targeted source scans found no remaining old `IFileStorageService.SaveAsync(IFormFile file, string category)` signature, no unscoped upload caller, no `LegacyAliases`, no `DefaultUnitSchematicKey`, no `ResolveDefaultSchematicKey`, no `defaultUnitSchematicKey`, and no stale `response-sedan` alias. | P2-15J closes the reported tenant file-storage and global-library assignment/defaulting gaps for current upload and schematic workflows. Product-owned static schematic assets remain global by design; tenant assignments remain tenant-owned records. Remaining tenant isolation work is governed by `P2-15K` automated tests, `P2-15L` controlled two-tenant verification, and `P2-15M` Phase 2 closure. |
| P2-15K | Done | Add automated tenant-isolation tests for current app workflows. | Implemented in commit `cf340ed` (`Add tenant isolation workflow tests`). Added a console-based automated tenant-isolation test harness under `tests/TenantIsolationTests` using an in-memory SQLite database and temp-only file storage. Test setup creates two tenants with overlapping company names, callsigns, staff identities, asset identifiers, checklist names, report data, upload paths, schematic assignments, readiness records, issues, tasks, and audit logs. Added a source hardening check in `VehicleSchematicAssignmentService.ResolveForVehicleAsync` so a cross-company vehicle object cannot resolve a schematic. No active database data, runtime files, generated files, logs, or app startup changes were made during implementation or commit. | `dotnet build` passed with 0 warnings and 0 errors. `dotnet run --project tests\TenantIsolationTests\TenantIsolationTests.csproj` passed all five tests: tenant workflow snapshots do not leak records; current user service rejects company mismatch; checklist publishing rejects foreign tenant inputs; schematic resolution is company bounded; tenant file storage requires company-scoped paths. | P2-15K closes the automated current-workflow tenant-isolation test requirement. Test/demo data lives only in test fixtures and does not seed runtime product data. Remaining tenant isolation work is governed by `P2-15L` controlled two-tenant browser/HTTP verification and `P2-15M` Phase 2 closure. |
| P2-15L | Done | Run controlled two-tenant browser/HTTP verification. | Completed after `P2-15H` through `P2-15K`. The active dev database was backed up before controlled fixture writes and restored after verification. A pending schema update for `ChecklistPublishScopes.CompanyId` was applied to the active dev database before verification; the corresponding SQLite-safe migration correction was committed as `f3cffd9` (`Fix SQLite tenant scope migration`). No runtime, generated, backup, log, or product database fixture data was committed. | `dotnet build --no-restore` passed with 0 warnings and 0 errors. Controlled browser/HTTP verification passed for two tenants with overlapping identifiers across company login, role login, branding, Home, registers, checklist register, live daily checks, checklist reports, schematic assignments, tasks, issues, uploads, audit visibility, and cross-tenant record access probes. Initial verifier failures for Ops `VehicleRegister` and Staff `DailyVehicleChecklist` were traced to the temporary verifier fixture missing saved granular permission rows; the verifier harness was corrected to seed default saved permissions for its temporary users, and the rerun passed with no failures. | P2-15L closes controlled browser/HTTP tenant-isolation verification for the current app workflows. Product permission enforcement was not weakened; the permission-row correction was confined to temporary verification fixture setup. `P2-15M` is now unblocked for Phase 2 tracker closure and remaining-risk reconciliation. |
| P2-15M | Done | Reconcile tracker evidence and close Phase 2. | Docs-only reconciliation completed after `P2-15A` through `P2-15L` were marked Done. Evidence reviewed: read-only audits `P2-15A` through `P2-15G`; implementation commits `4d669f0` (`P2-15H` tenant keys), `509d35a` (`P2-15I` tenant query/session scoping), `73ec695` (`P2-15J` tenant file storage and schematic defaults), `cf340ed` (`P2-15K` tenant-isolation tests), `f3cffd9` (`P2-15L` SQLite tenant-scope migration correction), and tracker evidence commit `e7a3f52` (`P2-15L` controlled verification evidence). No product source, database files, runtime files, generated files, staging, commit, deletion, or app startup were performed during `P2-15M`. | Phase 2 tenant/company source-of-truth scope may close for the current local app workflows. Evidence reviewed: implementation rows record successful `dotnet build` / `dotnet build --no-restore` runs with 0 warnings and 0 errors; the tenant isolation harness passed all five tests; controlled two-tenant browser/HTTP verification passed for company login, role login, branding, Home, registers, checklist register, live daily checks, checklist reports, schematic assignments, tasks, issues, uploads, audit visibility, and cross-tenant access probes; seed/fallback/startup checks found no normal path that recreates tenant records after the child-row fixes. | No remaining Phase 2 blockers are recorded for the current local tenant/company source-of-truth scope. Risks carried forward to later production phases: EF baseline/model snapshot and migration-history discipline still need a production migration decision; local SQLite and in-memory session are not production SaaS architecture and must be replaced by production tenant/session/provisioning/backup architecture; `NoOpFileSecurityScanner` is a local integration point and must be replaced by a real file-security scanning provider before production uploads; controlled verification covered current workflows, so every future feature must keep tenant scoping tests and browser verification. Phase 2 can close with these carried forward as later-phase production hardening requirements. |

## Tracker Update Rules

1. Add child rows before entering a phase gate.
2. Keep existing IDs stable after they are used.
3. If a row is split, leave the original row as a gate and add child rows below it.
4. If a row is obsolete, mark it `Blocked` with the reason. Do not delete it unless the user explicitly authorizes tracker cleanup.
5. Every source-changing row must name a verification method before work starts.
